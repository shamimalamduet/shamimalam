
import { ElectionCenter } from '../types';

/**
 * Hardcoded data is now secondary to the dynamic spreadsheet fetch.
 * Using empty array as initial fallback state in App.tsx.
 */
export const electionCenters: ElectionCenter[] = [];
